"""Live-agent Web workbench server (Wave 2).

Drives ``AgentRuntime.from_config`` — the SAME composition root the CLI and eval
use — and streams the agent event stream to the browser over SSE. localhost-only.
"""
